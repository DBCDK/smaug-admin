import React from 'react';

import ClientFormContainer from '../clientForm/clientFormContainer.component';

export default function CreateClientForm() {
  return (
      <ClientFormContainer />
  );
}
